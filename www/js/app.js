angular.module('Ubicla', ['ionic', 'leaflet-directive'])

.controller('mapCtrl', function($scope) {
	// var map = L.map('map', {
	//     center: [19.043925, -98.198204],
	//     zoom: 13,
	//     zoomControl: false,
	// });
	// L.tileLayer('http://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
	//     maxZoom: 18
	// }).addTo(map);
});